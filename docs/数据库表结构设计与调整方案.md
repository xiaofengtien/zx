# 数据库表结构设计与调整方案

## 目录
1. [概述](#概述)
2. [新增表结构](#新增表结构)
3. [现有表调整](#现有表调整)
4. [表关联关系](#表关联关系)
5. [后台功能模块](#后台功能模块)
6. [客户端功能模块](#客户端功能模块)
7. [同步机制](#同步机制)
8. [数据迁移方案](#数据迁移方案)

---

## 概述

### 设计目标
1. **试卷管理**：支持后台创建、编辑、删除试卷
2. **题目关联**：试卷与题目通过关联表建立关系
3. **媒体文件**：题目媒体文件直接关联，无需通过AppMaterial
4. **答题流程**：先生成试卷 → 查询题目 → 答题 → 同步结果
5. **离线支持**：SQLite存储试卷包（ZIP格式），支持离线答题

### 核心变更
- 新增 `paper` 表（试卷主表）
- 新增 `paper_question` 表（试卷-题目关联表）
- 新增 `question_media` 表（题目媒体文件表，替代AppMaterial的复杂设计）
- 调整 `app_user_paper_info` 表（增加paper_id字段）
- 调整 `question_category_business_settings` 表（初始化数据，默认类型5）

---

## 新增表结构

### 1. paper 表（试卷主表）

```sql
CREATE TABLE `paper` (
  `id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '试卷ID',
  `paper_name` VARCHAR(255) NOT NULL COMMENT '试卷名称',
  `paper_code` VARCHAR(100) UNIQUE COMMENT '试卷编码（用于同步，格式：PAPER_YYYYMMDD_序号）',
  `paper_desc` TEXT COMMENT '试卷描述',
  `business_type` INT NOT NULL DEFAULT 5 COMMENT '业务类型（5-题库）',
  `business_id` INT COMMENT '业务ID（关联question_category_business_settings.id）',
  `total_score` DECIMAL(10,2) DEFAULT 0 COMMENT '总分',
  `total_questions` INT DEFAULT 0 COMMENT '题目总数',
  `duration` INT COMMENT '考试时长（分钟）',
  
  -- 新增字段：独白语音相关
  `intro_audio_url` VARCHAR(500) COMMENT '开场独白音频URL',
  `intro_audio_path` VARCHAR(500) COMMENT '开场独白音频路径（服务器）',
  `intro_audio_duration` INT COMMENT '开场独白时长（秒）',
  `intro_text` TEXT COMMENT '开场独白文本内容（可选）',
  
  -- 新增字段：答题设置
  `auto_next_question` TINYINT DEFAULT 1 COMMENT '是否自动跳转下一题：1-是，0-否',
  `show_answer_immediately` TINYINT DEFAULT 0 COMMENT '是否立即显示答案：1-是，0-否',
  `allow_review` TINYINT DEFAULT 1 COMMENT '是否允许回顾：1-是，0-否',
  `question_read_duration` INT COMMENT '每题读题时长（秒，用于自动跳转）',
  
  -- 新增字段：同步相关
  `version` INT DEFAULT 1 COMMENT '版本号（用于增量同步）',
  `package_hash` VARCHAR(64) COMMENT '数据包哈希值（SHA256）',
  `package_size` BIGINT COMMENT '数据包大小（字节）',
  `last_package_time` DATETIME COMMENT '最后打包时间',
  
  -- 基础字段
  `status` TINYINT DEFAULT 1 COMMENT '状态：1-启用，0-禁用',
  `create_by` VARCHAR(64) DEFAULT '' COMMENT '创建者',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by` VARCHAR(64) DEFAULT '' COMMENT '更新者',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `remark` VARCHAR(500) DEFAULT '' COMMENT '备注',
  
  INDEX `idx_paper_code` (`paper_code`),
  INDEX `idx_business` (`business_type`, `business_id`),
  INDEX `idx_status` (`status`),
  INDEX `idx_version` (`version`)
) COMMENT='试卷表';
```

### 2. paper_question 表（试卷-题目关联表）

```sql
CREATE TABLE `paper_question` (
  `id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '关联ID',
  `paper_id` INT NOT NULL COMMENT '试卷ID',
  `question_id` INT NOT NULL COMMENT '题目ID',
  `sort_order` INT DEFAULT 0 COMMENT '排序号（题目在试卷中的顺序）',
  `score` DECIMAL(10,2) DEFAULT 0 COMMENT '分值',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  
  UNIQUE KEY `uk_paper_question` (`paper_id`, `question_id`),
  INDEX `idx_paper_id` (`paper_id`),
  INDEX `idx_question_id` (`question_id`),
  INDEX `idx_sort_order` (`paper_id`, `sort_order`)
) COMMENT='试卷题目关联表';
```

### 3. question_media 表（题目媒体文件表）

```sql
CREATE TABLE `question_media` (
  `id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '媒体ID',
  `question_id` INT NOT NULL COMMENT '题目ID',
  `media_type` TINYINT NOT NULL COMMENT '媒体类型：1-题目媒体，2-选项媒体',
  `option_id` INT COMMENT '选项ID（如果是选项媒体，关联question_answer.id）',
  `blank_area_id` INT COMMENT '完形填空区域ID（如果是完形填空的选项媒体）',
  `media_name` VARCHAR(255) NOT NULL COMMENT '媒体文件名',
  `media_path` VARCHAR(500) COMMENT '媒体路径（服务器路径，用于在线）',
  `media_url` VARCHAR(500) COMMENT '访问URL（CDN/OSS）',
  `media_data` BLOB COMMENT '媒体文件二进制数据（用于SQLite离线，小文件<50MB）',
  `media_size` INT COMMENT '文件大小（字节）',
  `media_format` VARCHAR(50) COMMENT '文件格式：jpg/png/mp3/mp4等',
  `media_duration` INT COMMENT '媒体时长（秒，音频/视频）',
  `is_compressed` TINYINT DEFAULT 0 COMMENT '是否压缩：1-是，0-否',
  `storage_type` TINYINT DEFAULT 0 COMMENT '存储类型：0-在线路径，1-SQLite BLOB，2-文件系统',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  
  INDEX `idx_question_id` (`question_id`),
  INDEX `idx_option_id` (`option_id`),
  INDEX `idx_blank_area_id` (`blank_area_id`),
  INDEX `idx_media_type` (`question_id`, `media_type`)
) COMMENT='题目媒体文件表';
```

### 4. paper_package 表（SQLite端：试卷包存储表）

```sql
-- SQLite 表结构
CREATE TABLE `paper_package` (
  `id` INTEGER PRIMARY KEY AUTOINCREMENT,
  `paper_id` INTEGER NOT NULL UNIQUE,
  `paper_code` TEXT NOT NULL UNIQUE,
  `package_data` BLOB COMMENT 'ZIP压缩包数据（如果<50MB直接存储）',
  `package_path` TEXT COMMENT 'ZIP包文件路径（如果>50MB，存储到文件系统）',
  `package_hash` TEXT NOT NULL COMMENT 'SHA256哈希值',
  `package_size` INTEGER NOT NULL,
  `storage_type` INTEGER DEFAULT 0 COMMENT '0-BLOB, 1-文件系统',
  `version` INTEGER DEFAULT 1,
  `sync_time` INTEGER NOT NULL COMMENT '同步时间戳',
  `is_active` INTEGER DEFAULT 1 COMMENT '是否激活：1-是，0-否',
  INDEX `idx_paper_id` (`paper_id`),
  INDEX `idx_paper_code` (`paper_code`)
) COMMENT='试卷包表（离线端）';
```

### 5. paper_index 表（SQLite端：试卷索引表）

```sql
-- SQLite 表结构（用于快速查询，不存储完整数据）
CREATE TABLE `paper_index` (
  `id` INTEGER PRIMARY KEY AUTOINCREMENT,
  `paper_id` INTEGER NOT NULL UNIQUE,
  `paper_code` TEXT NOT NULL,
  `paper_name` TEXT NOT NULL,
  `total_questions` INTEGER DEFAULT 0,
  `total_score` REAL DEFAULT 0,
  `duration` INTEGER,
  `intro_audio_url` TEXT,
  `intro_audio_duration` INTEGER,
  `version` INTEGER DEFAULT 1,
  `package_hash` TEXT,
  `sync_time` INTEGER,
  `is_active` INTEGER DEFAULT 1,
  INDEX `idx_paper_code` (`paper_code`)
) COMMENT='试卷索引表（离线端）';
```

### 6. 客户端答题结果表（SQLite端）

```sql
-- SQLite 表结构（与MySQL保持一致，用于离线存储）
CREATE TABLE `app_user_paper_info` (
  `id` INTEGER PRIMARY KEY AUTOINCREMENT,
  `paper_id` INTEGER NOT NULL,
  `paper_name` TEXT,
  `app_user_id` INTEGER NOT NULL,
  `start_time` INTEGER COMMENT '开始答题时间（时间戳）',
  `submit_time` INTEGER COMMENT '提交时间（时间戳）',
  `used_time` INTEGER COMMENT '用时（秒）',
  `total_score` REAL DEFAULT 0,
  `user_score` REAL DEFAULT 0,
  `correct_count` INTEGER DEFAULT 0,
  `wrong_count` INTEGER DEFAULT 0,
  `is_submit` INTEGER DEFAULT 0 COMMENT '是否提交：1-是，0-否',
  `sync_status` INTEGER DEFAULT 0 COMMENT '同步状态：0-未同步，1-已同步，2-同步失败',
  `sync_time` INTEGER COMMENT '同步时间（时间戳）',
  `create_time` INTEGER,
  `update_time` INTEGER,
  INDEX `idx_paper_id` (`paper_id`),
  INDEX `idx_app_user_id` (`app_user_id`),
  INDEX `idx_sync_status` (`sync_status`)
) COMMENT='用户试卷信息表（离线端）';

CREATE TABLE `app_user_paper_question_result` (
  `id` INTEGER PRIMARY KEY AUTOINCREMENT,
  `paper_id` INTEGER NOT NULL,
  `app_user_id` INTEGER NOT NULL,
  `question_id` INTEGER NOT NULL,
  `answer_ids` TEXT COMMENT '用户选择的答案ID（多个英文逗号隔开）',
  `user_answer` TEXT COMMENT '用户答案',
  `result` INTEGER COMMENT '结果：1-正确，0-错误',
  `question_sort` INTEGER COMMENT '题目排序',
  `answer_time` INTEGER COMMENT '答题时间（时间戳）',
  `time_spent` INTEGER COMMENT '用时（秒）',
  `is_reviewed` INTEGER DEFAULT 0 COMMENT '是否回顾过：1-是，0-否',
  `sync_status` INTEGER DEFAULT 0 COMMENT '同步状态：0-未同步，1-已同步',
  `create_time` INTEGER,
  INDEX `idx_paper_user` (`paper_id`, `app_user_id`),
  INDEX `idx_sync_status` (`sync_status`)
) COMMENT='用户试卷题目结果表（离线端）';

CREATE TABLE `app_user_paper_question_blank_result` (
  `id` INTEGER PRIMARY KEY AUTOINCREMENT,
  `paper_id` INTEGER NOT NULL,
  `app_user_id` INTEGER NOT NULL,
  `question_id` INTEGER NOT NULL,
  `blank_area_id` INTEGER,
  `blank_index` INTEGER COMMENT '空位序号',
  `answer_ids` TEXT COMMENT '用户选择的答案ID',
  `result` INTEGER COMMENT '结果：1-正确，0-错误',
  `answer_time` INTEGER COMMENT '答题时间（时间戳）',
  `time_spent` INTEGER COMMENT '用时（秒）',
  `sync_status` INTEGER DEFAULT 0 COMMENT '同步状态：0-未同步，1-已同步',
  `create_time` INTEGER,
  INDEX `idx_sync_status` (`sync_status`)
) COMMENT='用户完形填空答题结果表（离线端）';
```

---

## 现有表调整

### 1. app_user_paper_info 表调整

```sql
-- 新增字段
ALTER TABLE `app_user_paper_info` 
ADD COLUMN `paper_id` INT COMMENT '试卷ID（关联paper表）' AFTER `business_id`,
ADD COLUMN `start_time` DATETIME COMMENT '开始答题时间',
ADD COLUMN `submit_time` DATETIME COMMENT '提交时间',
ADD COLUMN `used_time` INT COMMENT '用时（秒）',
ADD COLUMN `total_score` DECIMAL(10,2) DEFAULT 0 COMMENT '总分',
ADD COLUMN `user_score` DECIMAL(10,2) DEFAULT 0 COMMENT '得分',
ADD COLUMN `correct_count` INT DEFAULT 0 COMMENT '正确题数',
ADD COLUMN `wrong_count` INT DEFAULT 0 COMMENT '错误题数',
ADD COLUMN `sync_status` TINYINT DEFAULT 0 COMMENT '同步状态：0-未同步，1-已同步，2-同步失败',
ADD COLUMN `sync_time` DATETIME COMMENT '同步时间',
ADD INDEX `idx_paper_id` (`paper_id`),
ADD INDEX `idx_app_user_id` (`appUserId`),
ADD INDEX `idx_sync_status` (`sync_status`);

-- 说明：
-- 1. paper_id：关联新的paper表，替代原来的business_id+business_type方式
-- 2. questionOrder字段保留，但主要用于兼容，新逻辑通过paper_question表查询
-- 3. 增加计时相关字段：start_time, submit_time, used_time
-- 4. 增加成绩相关字段：total_score, user_score, correct_count, wrong_count
-- 5. 增加同步状态字段：sync_status, sync_time（用于客户端同步到MySQL）
```

### 2. app_user_paper_question_result 表调整

```sql
-- 新增字段
ALTER TABLE `app_user_paper_question_result`
ADD COLUMN `answer_time` DATETIME COMMENT '答题时间',
ADD COLUMN `time_spent` INT COMMENT '用时（秒）',
ADD COLUMN `is_reviewed` TINYINT DEFAULT 0 COMMENT '是否回顾过：1-是，0-否',
ADD COLUMN `sync_status` TINYINT DEFAULT 0 COMMENT '同步状态：0-未同步，1-已同步',
ADD INDEX `idx_paper_user` (`paperId`, `appUserId`),
ADD INDEX `idx_sync_status` (`sync_status`);
```

### 3. app_user_paper_question_blank_result 表调整

```sql
-- 新增字段
ALTER TABLE `app_user_paper_question_blank_result`
ADD COLUMN `answer_time` DATETIME COMMENT '答题时间',
ADD COLUMN `time_spent` INT COMMENT '用时（秒）',
ADD COLUMN `sync_status` TINYINT DEFAULT 0 COMMENT '同步状态：0-未同步，1-已同步',
ADD INDEX `idx_sync_status` (`sync_status`);
```

### 4. question_category_business_settings 表调整

```sql
-- 初始化数据（默认类型为5）
-- 注意：此表不再需要维护页面，通过初始化脚本创建默认数据

-- 初始化脚本示例
INSERT INTO `question_category_business_settings` 
(`business_id`, `business_type`, `question_method`, `question_num`, `status`, `create_time`)
SELECT 
  qc.id AS business_id,
  5 AS business_type,  -- 默认类型为5
  1 AS question_method,  -- 默认出题方式
  10 AS question_num,  -- 默认题目数量
  1 AS status,
  NOW() AS create_time
FROM `question_category` qc
WHERE qc.status = 0  -- 只初始化启用状态的分类
  AND NOT EXISTS (
    SELECT 1 FROM `question_category_business_settings` qcbs 
    WHERE qcbs.business_id = qc.id AND qcbs.business_type = 5
  );
```

---

## 表关联关系

### 关联关系图

```
paper (试卷表)
  ├── paper_id → app_user_paper_info.paper_id (1:N)
  ├── business_id → question_category_business_settings.business_id (N:1)
  └── paper_id → paper_question.paper_id (1:N)
      └── question_id → question.id (N:1)
          ├── question_id → question_media.question_id (1:N)
          ├── id → question_answer.question_id (1:N)
          │   └── id → question_media.option_id (1:N)
          └── id → question_blank_area.question_id (1:N)
              └── id → question_media.blank_area_id (1:N)

app_user_paper_info (用户试卷信息)
  ├── paper_id → paper.id (N:1)
  ├── appUserId → student_archive.id (N:1)
  └── id → app_user_paper_question_result.paperId (1:N)
      └── questionId → question.id (N:1)

question_category (题目分类)
  ├── id → question.questionCategoryId (1:N)
  └── id → question_category_business_settings.business_id (1:1)
      └── business_id → paper.business_id (1:N)
```

### 关键关联说明

1. **试卷与题目**：通过 `paper_question` 表建立多对多关系
2. **试卷与业务设置**：`paper.business_id` → `question_category_business_settings.business_id`
3. **题目与媒体**：`question_media` 表直接关联题目，支持题目媒体和选项媒体
4. **用户试卷**：`app_user_paper_info.paper_id` 关联 `paper.id`，替代原来的 business_id+business_type

---

## 后台功能模块

### 1. 题库设置管理

**功能说明**：
- 管理题目分类（question_category）
- 初始化 `question_category_business_settings` 数据（默认类型5）
- 不再需要维护页面，通过初始化脚本自动创建

**相关表**：
- `question_category`
- `question_category_business_settings`

**接口设计**：
```java
// 初始化业务设置
@PostMapping("/question/category/initSettings")
public Result initCategorySettings();

// 查询分类树（用于题目维护）
@GetMapping("/question/category/tree")
public Result getCategoryTree();
```

### 2. 题目维护

**功能说明**：
- 题目的增删改查
- 题目媒体文件上传（直接关联到question_media表）
- 支持题目类型：单选题、多选题、判断题、填空题、排序题、完形填空

**相关表**：
- `question`
- `question_answer`
- `question_blank_area`
- `question_media`

**接口设计**：
```java
// 题目列表（分页）
@PostMapping("/question/list")
public Result listQuestions(@RequestBody QuestionPageBO pageBO);

// 题目详情
@GetMapping("/question/{id}")
public Result getQuestion(@PathVariable Integer id);

// 创建题目
@PostMapping("/question")
public Result createQuestion(@RequestBody QuestionBO questionBO);

// 更新题目
@PutMapping("/question")
public Result updateQuestion(@RequestBody QuestionBO questionBO);

// 删除题目
@DeleteMapping("/question/{ids}")
public Result deleteQuestion(@PathVariable Integer[] ids);

// 上传题目媒体文件
@PostMapping("/question/media/upload")
public Result uploadQuestionMedia(@RequestParam Integer questionId, 
                                  @RequestParam MultipartFile file);
```

### 3. 试卷出卷管理

**功能说明**：
- 试卷的增删改查
- 批量选择题库分类和题目（弹窗：左侧树形单选分类，右侧分页多选题目）
- 设置试卷属性（时长、分值、独白语音等）
- 生成试卷包（ZIP格式，包含题目JSON和媒体文件）

**相关表**：
- `paper`
- `paper_question`
- `question`
- `question_media`

**接口设计**：
```java
// 试卷列表（分页）
@PostMapping("/paper/list")
public Result listPapers(@RequestBody PaperPageBO pageBO);

// 试卷详情
@GetMapping("/paper/{id}")
public Result getPaper(@PathVariable Integer id);

// 创建试卷
@PostMapping("/paper")
public Result createPaper(@RequestBody PaperBO paperBO);

// 更新试卷
@PutMapping("/paper")
public Result updatePaper(@RequestBody PaperBO paperBO);

// 删除试卷
@DeleteMapping("/paper/{ids}")
public Result deletePaper(@PathVariable Integer[] ids);

// 批量选择题库分类和题目（弹窗接口）
@PostMapping("/paper/selectQuestions")
public Result selectQuestions(@RequestBody QuestionSelectBO selectBO);

// 生成试卷包（ZIP）
@PostMapping("/paper/{id}/generatePackage")
public Result generatePaperPackage(@PathVariable Integer id);
```

**弹窗设计**：
```
┌─────────────────────────────────────────┐
│  批量选择题库分类和题目                  │
├──────────┬──────────────────────────────┤
│          │  题目列表（分页）            │
│ 分类树    │  ┌──────────────────────┐  │
│ (单选)   │  │ ☑ 题目1               │  │
│ ├─ 语文  │  │ ☐ 题目2               │  │
│ ├─ 数学  │  │ ☑ 题目3               │  │
│ └─ 英语  │  │ ...                   │  │
│          │  └──────────────────────┘  │
│          │  [上一页] [下一页]          │
│          │  已选择：2题                │
├──────────┴──────────────────────────────┤
│  [取消]                    [确定]        │
└─────────────────────────────────────────┘
```

### 4. 答题结果查看

**功能说明**：
- 查看用户答题记录
- 查看答题详情（每题的对错、用时等）
- 统计分析（正确率、平均用时等）

**相关表**：
- `app_user_paper_info`
- `app_user_paper_question_result`
- `app_user_paper_question_blank_result`
- `paper`
- `question`

**接口设计**：
```java
// 答题记录列表（分页）
@PostMapping("/paper/result/list")
public Result listPaperResults(@RequestBody PaperResultPageBO pageBO);

// 答题详情
@GetMapping("/paper/result/{paperInfoId}")
public Result getPaperResult(@PathVariable Integer paperInfoId);

// 答题统计
@GetMapping("/paper/result/statistics")
public Result getPaperStatistics(@RequestParam Integer paperId);
```

---

## 客户端功能模块

### 1. 试卷列表

**功能说明**：
- 显示可用的试卷列表
- 支持离线查看（从SQLite读取）
- 显示试卷信息（名称、题目数、时长等）

**数据来源**：
- 在线：从MySQL的`paper`表查询
- 离线：从SQLite的`paper_index`表查询

### 2. 开始答题

**功能说明**：
1. 选择试卷
2. 播放开场独白语音（如果有）
3. 开始计时
4. 创建 `app_user_paper_info` 记录（SQLite）

**流程**：
```
选择试卷 → 加载试卷包（解压ZIP） → 播放独白 → 开始答题
```

### 3. 答题界面

**功能说明**：
- 显示当前题目（支持文本、图片、音频、视频）
- 光标锁定当前题目（动画效果：图标闪烁）
- 底部动态音频展示（播放题目音频）
- 自动跳转下一题（等语音播放完成后）
- 计时功能（显示剩余时间）

**界面设计**：
```
┌─────────────────────────────────────┐
│  试卷名称              [剩余时间]   │
├─────────────────────────────────────┤
│                                      │
│  [闪烁图标] 题目1/20                 │
│  ┌──────────────────────────────┐  │
│  │ 题目内容（文本/图片/音频/视频）│  │
│  └──────────────────────────────┘  │
│                                      │
│  选项：                              │
│  ○ A. 选项1                         │
│  ○ B. 选项2                         │
│  ○ C. 选项3                         │
│                                      │
│  [上一题]        [下一题]            │
├─────────────────────────────────────┤
│  [音频播放器] 正在播放题目音频...   │
└─────────────────────────────────────┘
```

**关键功能**：
1. **光标锁定**：当前题目图标闪烁动画
2. **自动跳转**：答题后自动跳转，等待下一题语音播放完成后再锁定
3. **计时**：倒计时显示，时间到自动提交

### 4. 答题结果

**功能说明**：
- 显示答题结果（总分、正确数、错误数）
- 显示每题的对错情况
- 支持回顾错题
- 实时显示结果（无需等待同步）

**数据存储**：
- 答题结果先保存到SQLite
- 联网时自动同步到MySQL

### 5. 结果同步

**功能说明**：
- 检测网络状态
- 联网时自动同步答题结果到MySQL
- 显示同步状态（已同步/未同步/同步失败）
- 支持手动触发同步

**同步数据**：
- `app_user_paper_info`（试卷信息）
- `app_user_paper_question_result`（题目结果）
- `app_user_paper_question_blank_result`（完形填空结果）

**接口设计**：
```java
// 同步答题结果
@PostMapping("/paper/result/sync")
public Result syncPaperResults(@RequestBody List<PaperResultSyncBO> results);
```

---

## 同步机制

### 1. 试卷包同步

**同步流程**：
```
启动时：
1. 检查本地是否有数据
   - 无数据 → 全量同步所有试卷包
   - 有数据 → 发送本地版本信息 → 增量同步

联网时：
1. 定期轮询（5分钟）或手动触发
2. 发送本地版本信息
3. 下载更新的试卷包
```

**接口设计**：
```java
// 检查同步状态
@PostMapping("/paper/sync/check")
public Result checkSyncStatus(@RequestBody SyncCheckRequest request);

// 下载试卷包（支持断点续传）
@GetMapping("/paper/sync/package/{paperCode}")
public ResponseEntity<Resource> downloadPackage(
    @PathVariable String paperCode,
    @RequestHeader(value = "Range", required = false) String range
);
```

### 2. 答题结果同步

**同步流程**：
```
答题完成后：
1. 保存到SQLite（app_user_paper_info等表）
2. 标记sync_status=0（未同步）

联网时：
1. 查询sync_status=0的记录
2. 批量同步到MySQL
3. 更新sync_status=1（已同步）
```

**接口设计**：
```java
// 批量同步答题结果
@PostMapping("/paper/result/sync")
public Result syncPaperResults(@RequestBody List<PaperResultSyncBO> results);
```

---

## 数据迁移方案

### 1. 现有数据迁移

**迁移步骤**：

1. **迁移试卷数据**：
```sql
-- 从app_user_paper_info提取试卷信息，创建paper记录
INSERT INTO `paper` (
  `paper_name`, `business_type`, `business_id`, 
  `total_questions`, `status`, `create_time`
)
SELECT DISTINCT
  `paper_name`,
  `business_type`,
  `business_id`,
  LENGTH(`question_order`) - LENGTH(REPLACE(`question_order`, ',', '')) + 1 AS total_questions,
  `status`,
  `create_time`
FROM `app_user_paper_info`
WHERE `paper_name` IS NOT NULL;
```

2. **迁移试卷-题目关联**：
```sql
-- 从app_user_paper_info的questionOrder字段解析题目ID，创建paper_question记录
-- 需要编写存储过程或Java代码处理
```

3. **更新app_user_paper_info**：
```sql
-- 更新paper_id字段
UPDATE `app_user_paper_info` a
INNER JOIN `paper` p 
  ON a.business_type = p.business_type 
  AND a.business_id = p.business_id
  AND a.paper_name = p.paper_name
SET a.paper_id = p.id;
```

### 2. 初始化question_category_business_settings

```sql
-- 为所有启用的题目分类创建默认业务设置
INSERT INTO `question_category_business_settings` 
(`business_id`, `business_type`, `question_method`, `question_num`, `status`, `create_time`)
SELECT 
  qc.id AS business_id,
  5 AS business_type,
  1 AS question_method,
  10 AS question_num,
  1 AS status,
  NOW() AS create_time
FROM `question_category` qc
WHERE qc.status = 0
  AND NOT EXISTS (
    SELECT 1 FROM `question_category_business_settings` qcbs 
    WHERE qcbs.business_id = qc.id AND qcbs.business_type = 5
  );
```

---

## 总结

### 关键变更点

1. **新增paper表**：统一管理试卷，支持独白语音、计时等功能
2. **新增paper_question表**：建立试卷与题目的关联关系
3. **新增question_media表**：直接关联题目媒体文件，简化设计
4. **调整app_user_paper_info**：增加paper_id、计时、成绩、同步状态等字段
5. **初始化question_category_business_settings**：默认类型5，无需维护页面

### 下一步工作

1. ✅ 确认表结构设计
2. ⏳ 实现后台维护页面（题库设置、题目维护、试卷出卷）
3. ⏳ 实现客户端答题页面（计时、动画、自动跳转）
4. ⏳ 实现同步机制（试卷包同步、答题结果同步）
5. ⏳ 数据迁移脚本

---

## 客户端SQLite表结构补充

### 完整的客户端表结构

客户端需要以下表来支持离线功能：

1. **paper_package** - 试卷包存储表（已定义）
2. **paper_index** - 试卷索引表（已定义）
3. **app_user_paper_info** - 用户试卷信息表（已定义）
4. **app_user_paper_question_result** - 用户题目结果表（已定义）
5. **app_user_paper_question_blank_result** - 用户完形填空结果表（已定义）
6. **student_archive** - 学员档案表（已存在）
7. **dict_data** - 字典数据表（已存在）

### 客户端初始化脚本（启动时自动执行）

**重要说明**：试卷相关表的初始化应该和现有表（student_archive、dict_data等）一样，在应用启动时自动执行，无需手动触发。

**实现方式**：在 `db.js` 的 `initTables()` 方法中调用 `initPaperTables()`，这样在应用启动时会自动初始化所有表。

```javascript
// exam-client/src/database/db.js

class DB {
  constructor() {
    // ... 数据库连接代码 ...
    
    // 初始化表结构（自动调用，无需手动触发）
    this.initTables()  // ← 启动时自动调用
  }

  initTables() {
    console.log('开始初始化数据库表结构...')
    
    // 1. 基础表（学员、字典等）
    this.initBasicTables()
    
    // 2. 试卷相关表（新增，自动初始化）
    this.initPaperTables()  // ← 自动调用，无需手动触发
    
    // 3. 创建所有索引
    this.createIndexes()
    
    console.log('数据库表结构初始化完成')
  }

  /**
   * 初始化基础表（学员、字典等）
   */
  initBasicTables() {
    // 现有的表初始化代码...
    this.db.exec(`CREATE TABLE IF NOT EXISTS student_credentials (...)`)
    this.db.exec(`CREATE TABLE IF NOT EXISTS student_papers (...)`)
    this.db.exec(`CREATE TABLE IF NOT EXISTS dict_data (...)`)
    this.db.exec(`CREATE TABLE IF NOT EXISTS student_archive (...)`)
  }

  /**
   * 初始化试卷相关表（新增）
   * 说明：此方法在 initTables() 中自动调用，启动时自动执行，无需手动触发
   */
  initPaperTables() {
    // 试卷包表
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS paper_package (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        paper_id INTEGER NOT NULL UNIQUE,
        paper_code TEXT NOT NULL UNIQUE,
        package_data BLOB,
        package_path TEXT,
        package_hash TEXT NOT NULL,
        package_size INTEGER NOT NULL,
        storage_type INTEGER DEFAULT 0,
        version INTEGER DEFAULT 1,
        sync_time INTEGER NOT NULL,
        is_active INTEGER DEFAULT 1
      )
    `);
    
    // 试卷索引表
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS paper_index (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        paper_id INTEGER NOT NULL UNIQUE,
        paper_code TEXT NOT NULL,
        paper_name TEXT NOT NULL,
        total_questions INTEGER DEFAULT 0,
        total_score REAL DEFAULT 0,
        duration INTEGER,
        intro_audio_url TEXT,
        intro_audio_duration INTEGER,
        version INTEGER DEFAULT 1,
        package_hash TEXT,
        sync_time INTEGER,
        is_active INTEGER DEFAULT 1
      )
    `);
    
    // 用户试卷信息表
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS app_user_paper_info (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        paper_id INTEGER NOT NULL,
        paper_name TEXT,
        app_user_id INTEGER NOT NULL,
        start_time INTEGER,
        submit_time INTEGER,
        used_time INTEGER,
        total_score REAL DEFAULT 0,
        user_score REAL DEFAULT 0,
        correct_count INTEGER DEFAULT 0,
        wrong_count INTEGER DEFAULT 0,
        is_submit INTEGER DEFAULT 0,
        sync_status INTEGER DEFAULT 0,
        sync_time INTEGER,
        create_time INTEGER,
        update_time INTEGER
      )
    `);
    
    // 用户题目结果表
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS app_user_paper_question_result (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        paper_id INTEGER NOT NULL,
        app_user_id INTEGER NOT NULL,
        question_id INTEGER NOT NULL,
        answer_ids TEXT,
        user_answer TEXT,
        result INTEGER,
        question_sort INTEGER,
        answer_time INTEGER,
        time_spent INTEGER,
        is_reviewed INTEGER DEFAULT 0,
        sync_status INTEGER DEFAULT 0,
        create_time INTEGER
      )
    `);
    
    // 用户完形填空结果表
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS app_user_paper_question_blank_result (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        paper_id INTEGER NOT NULL,
        app_user_id INTEGER NOT NULL,
        question_id INTEGER NOT NULL,
        blank_area_id INTEGER,
        blank_index INTEGER,
        answer_ids TEXT,
        result INTEGER,
        answer_time INTEGER,
        time_spent INTEGER,
        sync_status INTEGER DEFAULT 0,
        create_time INTEGER
      )
    `);
  }

  /**
   * 创建所有索引
   */
  createIndexes() {
    // 基础表索引（现有）
    this.db.exec(`CREATE INDEX IF NOT EXISTS idx_student_account ON student_credentials(student_account)`);
    // ... 其他现有索引 ...
    
    // 试卷相关表索引（新增）
    this.db.exec(`CREATE INDEX IF NOT EXISTS idx_paper_package_code ON paper_package(paper_code)`);
    this.db.exec(`CREATE INDEX IF NOT EXISTS idx_paper_index_code ON paper_index(paper_code)`);
    this.db.exec(`CREATE INDEX IF NOT EXISTS idx_paper_info_user ON app_user_paper_info(app_user_id)`);
    this.db.exec(`CREATE INDEX IF NOT EXISTS idx_paper_info_sync ON app_user_paper_info(sync_status)`);
    this.db.exec(`CREATE INDEX IF NOT EXISTS idx_paper_result_user ON app_user_paper_question_result(paper_id, app_user_id)`);
    this.db.exec(`CREATE INDEX IF NOT EXISTS idx_paper_result_sync ON app_user_paper_question_result(sync_status)`);
    this.db.exec(`CREATE INDEX IF NOT EXISTS idx_paper_blank_sync ON app_user_paper_question_blank_result(sync_status)`);
  }
}
```

**执行流程**：
```
应用启动
  ↓
app.whenReady()
  ↓
initDatabase()  // main.js 中调用
  ↓
new Database()  // 创建 DB 实例
  ↓
constructor()  // 自动执行
  ↓
this.initTables()  // 自动调用
  ↓
├─ initBasicTables()      // 基础表（学员、字典）
├─ initPaperTables()      // 试卷相关表（自动初始化）
└─ createIndexes()        // 所有索引
  ↓
表创建完成（自动完成，无需手动触发）
```

**关键点**：
- ✅ **自动执行**：应用启动时自动初始化所有表
- ✅ **无需手动触发**：和现有表一样，用户无需任何操作
- ✅ **使用 IF NOT EXISTS**：表已存在时不会报错
- ✅ **模块化设计**：代码清晰，易于维护

**详细分析文档**：请参考 `docs/SQLite表初始化机制分析.md`

## 附录

### A. Paper表字段说明

| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| intro_audio_url | VARCHAR(500) | 开场独白音频URL | 否 |
| intro_audio_path | VARCHAR(500) | 开场独白音频路径 | 否 |
| intro_audio_duration | INT | 开场独白时长（秒） | 否 |
| intro_text | TEXT | 开场独白文本内容 | 否 |
| auto_next_question | TINYINT | 是否自动跳转下一题 | 是（默认1） |
| show_answer_immediately | TINYINT | 是否立即显示答案 | 是（默认0） |
| allow_review | TINYINT | 是否允许回顾 | 是（默认1） |
| question_read_duration | INT | 每题读题时长（秒） | 否 |

### B. 同步状态说明

| sync_status | 说明 |
|-------------|------|
| 0 | 未同步 |
| 1 | 已同步 |
| 2 | 同步失败 |

### C. 存储类型说明

| storage_type | 说明 |
|-------------|------|
| 0 | SQLite BLOB（<50MB） |
| 1 | 文件系统（≥50MB） |

---

**文档版本**：v1.0  
**最后更新**：2024-01-XX  
**作者**：系统设计

