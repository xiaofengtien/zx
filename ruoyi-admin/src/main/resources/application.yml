# 项目相关配置
ruoyi:
  # 名称
  name: RuoYi
  # 版本
  version: 3.9.0
  # 版权年份
  copyrightYear: 2025
  # 文件路径 示例（ Windows配置D:/ruoyi/uploadPath，Linux配置 /home/ruoyi/uploadPath）
  profile: D:/ruoyi/uploadPath
  # 获取ip地址开关
  addressEnabled: false
  # 验证码类型 math 数字计算 char 字符验证
  captchaType: math

# 开发环境配置
server:
  # 服务器的HTTP端口，默认为8080
  port: 8080
  servlet:
    # 应用的访问路径
    context-path: /
  tomcat:
    # tomcat的URI编码
    uri-encoding: UTF-8
    # 连接数满后的排队数，默认为100
    accept-count: 1000
    threads:
      # tomcat最大线程数，默认为200
      max: 800
      # Tomcat启动初始化的线程数，默认值10
      min-spare: 100
    # POST请求最大大小（单个文件最大10MB）
    max-post-size: 10MB

# 日志配置
logging:
  level:
    com.ruoyi: debug
    org.springframework: warn

# 用户配置
user:
  password:
    # 密码最大错误次数
    maxRetryCount: 5
    # 密码锁定时间（默认10分钟）
    lockTime: 10

# Spring配置
spring:
  # 资源信息
  messages:
    # 国际化资源文件路径
    basename: i18n/messages
  profiles:
    active: druid
  # 文件上传
  servlet:
    multipart:
      # 单个文件大小限制（音频文件最大100MB）
      max-file-size: 100MB
      # 设置总上传的文件大小限制（单个请求最大100MB）
      max-request-size: 100MB
  # 服务模块
  devtools:
    restart:
      # 热部署开关
      enabled: true
  # redis 配置
  redis:
    # 地址
    host: localhost
    # 端口，默认为6379
    port: 6379
    # 数据库索引
    database: 0
    # 密码
    password: 123456
    # 连接超时时间
    timeout: 10s
    lettuce:
      pool:
        # 连接池中的最小空闲连接
        min-idle: 0
        # 连接池中的最大空闲连接
        max-idle: 8
        # 连接池的最大数据库连接数
        max-active: 8
        # #连接池最大阻塞等待时间（使用负值表示没有限制）
        max-wait: -1ms

# token配置
token:
  # 令牌自定义标识
  header: Authorization
  # 令牌密钥
  secret: abcdefghijklmnopqrstuvwxyz
  # 令牌有效期（默认30分钟）
  expireTime: 30

# MyBatis配置
mybatis:
  # 搜索指定包别名
  typeAliasesPackage: com.ruoyi.**.domain
  # 配置mapper的扫描，找到所有的mapper.xml映射文件
  mapperLocations: classpath*:mapper/**/*Mapper.xml
  # 加载全局的配置文件
  configLocation: classpath:mybatis/mybatis-config.xml

# PageHelper分页插件
pagehelper:
  helperDialect: mysql
  supportMethodsArguments: true
  params: count=countSql

# Swagger配置
swagger:
  # 是否开启swagger
  enabled: true
  # 请求前缀
  pathMapping: /dev-api

# 防盗链配置
referer:
  # 防盗链开关
  enabled: false
  # 允许的域名列表
  allowed-domains: localhost,127.0.0.1,ruoyi.vip,www.ruoyi.vip

# 防止XSS攻击
xss:
  # 过滤开关
  enabled: true
  # 排除链接（多个用逗号分隔）
  excludes: /system/notice
  # 匹配链接
  urlPatterns: /system/*,/monitor/*,/tool/*

# 试卷包生成任务配置
paper:
  package:
    task:
      # 是否使用Redis存储任务状态（true=使用Redis，false=仅使用内存）
      use-redis: true
      # 任务过期时间（秒），默认1小时
      expire-time: 3600
  # 试卷智能导入配置
  import:
    # Python 解析服务地址
    parse-service-url: http://localhost:8088
    # 音频切分策略 (vad/asr)
    audio-split-strategy: vad
    # 最小静音时长 (ms)
    min-silence-len: 3000



aliyun:
  oss:
    endpoint: oss-cn-shanghai.aliyuncs.com  # 上海区域（与 Python ASR 服务相同）
    accessKeyId: LTAI5tHZ7Jid674gjy3m28XL         # 与 Python 服务相同的 AK
    accessKeySecret: msjnMVfjntJb1VBXykdpqwazTukdpg # 与 Python 服务相同的 SK
    bucketName: zx-exam-paper          # 考试相关文件专用存储桶（私有）
    urlPrefix: https://zx-exam-paper.oss-cn-shanghai.aliyuncs.com/ # 阿里云默认域名（永久有效）
    cdn:   # 不使用 CDN，直接使用阿里云默认域名
    temp:
      endpoint: sts.cn-shanghai.aliyuncs.com  # STS 服务（上海区域）
      accessKeyId: LTAI5tHZ7Jid674gjy3m28XL         # 与主配置相同
      accessKeySecret: msjnMVfjntJb1VBXykdpqwazTukdpg # 与主配置相同
      roleArn:       # 如需 STS 临时凭证，需配置 RAM 角色
      roleSessionName: tempToken
      regionId: cn-shanghai

# OSS提供商配置（qiniu-七牛云，aliyun-阿里云）
oss:
  provider: aliyun  # 使用阿里云（默认域名永久有效，无需绑定自定义域名）

# 七牛云OSS配置（使用自定义域名）
qiniu:
  oss:
    accessKey: 1KhnqEk0TJNusTEAGokJrfYVTzwaBPh9nhPzFQZu
    secretKey: kaAp8EBC4qMP9cseSOpSbzL1nN7skrOPQ5LIHwRK
    bucketName: zx-exam-sg
    domain: aaron2025.xyz
    urlPrefix: http://aaron2025.xyz/
    cdn:
    region: as0  # 东南亚区域